{% extends 'base.html.twig' %}
{% block title %}Utilisateurs{% endblock %}
{% block page_title %}Utilisateurs{% endblock %}
{% block page_subtitle %}{{ users|length }} utilisateur{{ users|length > 1 ? 's' : '' }}{% endblock %}

{% block page_actions %}
    <button onclick="openModal('addModal')" class="inline-flex items-center gap-2 rounded-lg bg-ardoise hover:bg-ardoise-clair px-4 py-2.5 text-sm font-medium text-white">
        <i class="fas fa-plus text-[12px]"></i> Ajouter
    </button>
{% endblock %}

{% block body %}
    <div class="rounded-2xl border border-trait bg-white">
        <table class="w-full text-sm">
            <thead>
            <tr class="border-b border-trait-fin bg-voile/50">
                <th class="py-3 px-5 text-left text-xs font-medium uppercase text-ardoise-faible">Identifiant</th>
                <th class="py-3 px-3 text-center text-xs font-medium uppercase text-ardoise-faible">Rôle</th>
                <th class="py-3 px-3 text-center text-xs font-medium uppercase text-ardoise-faible">Statut</th>
                <th class="py-3 px-5 text-center text-xs font-medium uppercase text-ardoise-faible w-28">Actions</th>
            </tr>
            </thead>
            <tbody class="divide-y divide-trait-fin">
            {% for u in users %}
                <tr class="hover:bg-voile/50 {{ not u.active ? 'opacity-50' }}">
                    <td class="py-3.5 px-5 font-medium text-ardoise">{{ u.username }}</td>
                    <td class="py-3.5 px-3 text-center">
                        <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-2xs font-medium {{ u.role == 'admin' ? 'bg-purple-50 text-purple-600' : (u.role == 'editor' ? 'bg-voile text-laiton-patine' : 'bg-voile text-ardoise-doux') }}">{{ u.role }}</span>
                    </td>
                    <td class="py-3.5 px-3 text-center">
                        {% if u.active %}
                            <span class="inline-flex items-center rounded-full bg-pousse-voile px-2 py-0.5 text-2xs font-medium text-pousse">Actif</span>
                        {% else %}
                            <span class="inline-flex items-center rounded-full bg-sang-voile px-2 py-0.5 text-2xs font-medium text-sang">Inactif</span>
                        {% endif %}
                    </td>
                    <td class="py-3.5 px-5">
                        <div class="flex items-center justify-center gap-1">
                            <button type="button"
                                    onclick="openEditUserModal({{ u.id }}, '{{ u.username|e('js') }}', '{{ u.role|e('js') }}', {{ u.active ? 'true' : 'false' }})"
                                    class="w-8 h-8 inline-flex items-center justify-center rounded-lg text-ardoise-faible hover:text-laiton-patine hover:bg-voile transition-colors"
                                    title="Modifier">
                                <i class="fas fa-pen text-xs"></i>
                            </button>
                            {% if u.id != app.user.id %}
                                <form method="post" action="{{ path('app_admin_user_delete', {id: u.id}) }}" onsubmit="return confirm('Supprimer l’utilisateur « {{ u.username|e('js') }} » ?')" class="inline">
                                    <button class="w-8 h-8 inline-flex items-center justify-center rounded-lg text-ardoise-faible hover:text-sang hover:bg-sang-voile transition-colors" title="Supprimer">
                                        <i class="fas fa-trash text-xs"></i>
                                    </button>
                                </form>
                            {% else %}
                                <span class="w-8 h-8 inline-flex items-center justify-center rounded-lg text-ardoise-faible cursor-not-allowed" title="Vous ne pouvez pas supprimer votre propre compte">
                                    <i class="fas fa-trash text-xs"></i>
                                </span>
                            {% endif %}
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    {# ══ MODAL AJOUTER ══ #}
    <div id="addModal" class="fixed inset-0 z-[9999] hidden items-center justify-center bg-black/50">
        <form method="post" action="{{ path('app_admin_user_create') }}" class="bg-white rounded-2xl shadow-xl w-full max-w-sm mx-4">
            <div class="px-6 py-4 border-b border-trait-fin flex items-center justify-between">
                <h3 class="font-semibold text-ardoise">Nouvel utilisateur</h3>
                <button type="button" onclick="closeModal('addModal')" class="text-ardoise-faible hover:text-ardoise-doux"><i class="fas fa-times"></i></button>
            </div>
            <div class="p-6 space-y-4">
                <div><label class="block text-sm font-medium text-ardoise-doux mb-1.5">Identifiant *</label><input type="text" name="username" required minlength="3" class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine"></div>
                <div><label class="block text-sm font-medium text-ardoise-doux mb-1.5">Mot de passe * (min 6)</label><input type="password" name="password" required minlength="6" class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine"></div>
                <div><label class="block text-sm font-medium text-ardoise-doux mb-1.5">Rôle</label>
                    <select name="role" class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine bg-white">
                        <option value="reader">Lecteur</option>
                        <option value="editor" selected>Éditeur</option>
                        <option value="admin">Admin</option>
                    </select>
                </div>
            </div>
            <div class="px-6 py-4 border-t border-trait-fin flex justify-end gap-3">
                <button type="button" onclick="closeModal('addModal')" class="rounded-lg border border-trait px-4 py-2 text-sm text-ardoise-doux hover:bg-voile">Annuler</button>
                <button type="submit" class="rounded-lg bg-ardoise hover:bg-ardoise-clair px-5 py-2 text-sm font-medium text-white">Créer</button>
            </div>
        </form>
    </div>

    {# ══ MODAL MODIFIER ══ #}
    <div id="editModal" class="fixed inset-0 z-[9999] hidden items-center justify-center bg-black/50">
        <form id="editUserForm" method="post" action="" class="bg-white rounded-2xl shadow-xl w-full max-w-sm mx-4">
            <div class="px-6 py-4 border-b border-trait-fin flex items-center justify-between">
                <h3 class="font-semibold text-ardoise">Modifier l'utilisateur</h3>
                <button type="button" onclick="closeModal('editModal')" class="text-ardoise-faible hover:text-ardoise-doux"><i class="fas fa-times"></i></button>
            </div>
            <div class="p-6 space-y-4">
                <div>
                    <label class="block text-sm font-medium text-ardoise-doux mb-1.5">Identifiant</label>
                    <input type="text" id="editUserName" disabled class="w-full rounded-lg border border-trait bg-voile px-4 py-2.5 text-sm text-ardoise-doux outline-none">
                    <p class="text-xs text-ardoise-faible mt-1">L'identifiant n'est pas modifiable</p>
                </div>
                <div>
                    <label class="block text-sm font-medium text-ardoise-doux mb-1.5">Rôle</label>
                    <select id="editUserRole" name="role" class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine bg-white">
                        <option value="reader">Lecteur</option>
                        <option value="editor">Éditeur</option>
                        <option value="admin">Admin</option>
                    </select>
                </div>
                <div>
                    <label class="block text-sm font-medium text-ardoise-doux mb-1.5">Nouveau mot de passe (min 6)</label>
                    <input type="password" name="new_password" minlength="6" placeholder="Laisser vide pour ne pas changer" class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine">
                </div>
                <label class="flex items-center gap-2.5 cursor-pointer select-none">
                    <input type="checkbox" id="editUserActive" name="is_active" value="1" class="w-4 h-4 rounded border-trait text-laiton-patine focus:ring-laiton/40">
                    <span class="text-sm text-ardoise-doux">Compte actif</span>
                </label>
            </div>
            <div class="px-6 py-4 border-t border-trait-fin flex justify-end gap-3">
                <button type="button" onclick="closeModal('editModal')" class="rounded-lg border border-trait px-4 py-2 text-sm text-ardoise-doux hover:bg-voile">Annuler</button>
                <button type="submit" class="rounded-lg bg-ardoise hover:bg-ardoise-clair px-5 py-2 text-sm font-medium text-white"><i class="fas fa-save text-[12px] mr-1.5"></i> Enregistrer</button>
            </div>
        </form>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        const USER_EDIT_URL = '{{ path('app_admin_user_update', {id: '__ID__'}) }}';
        function openEditUserModal(id, username, role, active) {
            document.getElementById('editUserName').value     = username;
            document.getElementById('editUserRole').value     = role;
            document.getElementById('editUserActive').checked = active;
            document.getElementById('editUserForm').action    = USER_EDIT_URL.replace('__ID__', id);
            openModal('editModal');
        }
    </script>
{% endblock %}
