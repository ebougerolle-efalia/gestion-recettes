{% extends 'base.html.twig' %}
{% block title %}Familles{% endblock %}
{% block page_title %}Familles de recettes{% endblock %}
{% block page_subtitle %}{{ families|length }} famille{{ families|length > 1 ? 's' : '' }}{% endblock %}

{% block page_actions %}
    <button onclick="openModal('addModal')" class="inline-flex items-center gap-2 rounded-lg bg-ardoise hover:bg-ardoise-clair px-4 py-2.5 text-sm font-medium text-white">
        <i class="fas fa-plus text-[11px]"></i> Ajouter
    </button>
{% endblock %}

{% block body %}
    <div class="rounded-2xl border border-trait bg-white">
        <table class="w-full text-sm">
            <thead>
            <tr class="border-b border-trait-fin bg-voile/50">
                <th class="py-3 px-5 text-left text-xs font-medium uppercase text-ardoise-faible">Nom</th>
                <th class="py-3 px-3 text-center text-xs font-medium uppercase text-ardoise-faible w-24">Ordre</th>
                <th class="py-3 px-5 text-center text-xs font-medium uppercase text-ardoise-faible w-28">Actions</th>
            </tr>
            </thead>
            <tbody class="divide-y divide-trait-fin">
            {% for f in families %}
                <tr class="hover:bg-voile/50">
                    <td class="py-3.5 px-5 font-medium text-ardoise">{{ f.name }}</td>
                    <td class="py-3.5 px-3 text-center">
                        <span class="inline-flex items-center justify-center w-8 h-8 rounded-lg bg-voile text-ardoise-doux text-sm font-semibold">{{ f.sortOrder }}</span>
                    </td>
                    <td class="py-3.5 px-5">
                        <div class="flex items-center justify-center gap-1">
                            <button type="button"
                                    onclick="openEditFamilyModal({{ f.id }}, '{{ f.name|e('js') }}', {{ f.sortOrder }})"
                                    class="w-8 h-8 inline-flex items-center justify-center rounded-lg text-ardoise-faible hover:text-laiton-patine hover:bg-voile transition-colors"
                                    title="Modifier">
                                <i class="fas fa-pen text-xs"></i>
                            </button>
                            <form method="post" action="{{ path('app_admin_family_delete', {id: f.id}) }}" onsubmit="return confirm('Supprimer la famille « {{ f.name|e('js') }} » ?')" class="inline">
                                <button class="w-8 h-8 inline-flex items-center justify-center rounded-lg text-ardoise-faible hover:text-sang hover:bg-sang-voile transition-colors" title="Supprimer">
                                    <i class="fas fa-trash text-xs"></i>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            {% else %}
                <tr><td colspan="3" class="py-12 text-center text-ardoise-faible">Aucune famille</td></tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    {# ══ MODAL AJOUTER ══ #}
    <div id="addModal" class="fixed inset-0 z-[9999] hidden items-center justify-center bg-black/50">
        <form method="post" action="{{ path('app_admin_family_create') }}" class="bg-white rounded-2xl shadow-xl w-full max-w-sm mx-4">
            <div class="px-6 py-4 border-b border-trait-fin flex items-center justify-between">
                <h3 class="font-semibold text-ardoise">Nouvelle famille</h3>
                <button type="button" onclick="closeModal('addModal')" class="text-ardoise-faible hover:text-ardoise-doux"><i class="fas fa-times"></i></button>
            </div>
            <div class="p-6 space-y-4">
                <div>
                    <label class="block text-sm font-medium text-ardoise-doux mb-1.5">Nom *</label>
                    <input type="text" name="name" required class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine focus:ring-1 focus:ring-laiton/30">
                </div>
                <div>
                    <label class="block text-sm font-medium text-ardoise-doux mb-1.5">Ordre d'affichage</label>
                    <input type="number" name="sort_order" value="{{ families|length * 10 }}" min="0" class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine focus:ring-1 focus:ring-laiton/30">
                </div>
            </div>
            <div class="px-6 py-4 border-t border-trait-fin flex justify-end gap-3">
                <button type="button" onclick="closeModal('addModal')" class="rounded-lg border border-trait px-4 py-2 text-sm text-ardoise-doux hover:bg-voile">Annuler</button>
                <button type="submit" class="rounded-lg bg-ardoise hover:bg-ardoise-clair px-5 py-2 text-sm font-medium text-white">Créer</button>
            </div>
        </form>
    </div>

    {# ══ MODAL MODIFIER ══ #}
    <div id="editModal" class="fixed inset-0 z-[9999] hidden items-center justify-center bg-black/50">
        <form id="editFamilyForm" method="post" action="" class="bg-white rounded-2xl shadow-xl w-full max-w-sm mx-4">
            <div class="px-6 py-4 border-b border-trait-fin flex items-center justify-between">
                <h3 class="font-semibold text-ardoise">Modifier la famille</h3>
                <button type="button" onclick="closeModal('editModal')" class="text-ardoise-faible hover:text-ardoise-doux"><i class="fas fa-times"></i></button>
            </div>
            <div class="p-6 space-y-4">
                <div>
                    <label class="block text-sm font-medium text-ardoise-doux mb-1.5">Nom *</label>
                    <input type="text" id="editFamilyName" name="name" required class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine focus:ring-1 focus:ring-laiton/30">
                </div>
                <div>
                    <label class="block text-sm font-medium text-ardoise-doux mb-1.5">Ordre d'affichage</label>
                    <input type="number" id="editFamilyOrder" name="sort_order" min="0" class="w-full rounded-lg border border-trait px-4 py-2.5 text-sm outline-none focus:border-laiton-patine focus:ring-1 focus:ring-laiton/30">
                    <p class="text-xs text-ardoise-faible mt-1">Les familles sont triées par ordre croissant</p>
                </div>
            </div>
            <div class="px-6 py-4 border-t border-trait-fin flex justify-end gap-3">
                <button type="button" onclick="closeModal('editModal')" class="rounded-lg border border-trait px-4 py-2 text-sm text-ardoise-doux hover:bg-voile">Annuler</button>
                <button type="submit" class="rounded-lg bg-ardoise hover:bg-ardoise-clair px-5 py-2 text-sm font-medium text-white"><i class="fas fa-save text-[10px] mr-1.5"></i> Enregistrer</button>
            </div>
        </form>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        const FAMILY_EDIT_URL = '{{ path('app_admin_family_update', {id: '__ID__'}) }}';
        function openEditFamilyModal(id, name, order) {
            document.getElementById('editFamilyName').value  = name;
            document.getElementById('editFamilyOrder').value = order;
            document.getElementById('editFamilyForm').action = FAMILY_EDIT_URL.replace('__ID__', id);
            openModal('editModal');
        }
    </script>
{% endblock %}
