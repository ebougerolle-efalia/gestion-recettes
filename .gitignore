# === Symfony ===
/.env.local
/.env.local.php
/.env.*.local
/config/secrets/prod/prod.decrypt.private.php
/public/bundles/
/var/
/vendor/

# === Base de données SQLite ===
/var/data/*.db

# === Uploads clients (logos, etc. — régénérés au déploiement) ===
/public/uploads/

# === Configuration d'exploitation (déplacée vers gestion-recettes-ops) ===
# Filet de sécurité : si un setup.conf traîne encore ici sur ce poste, il ne
# doit jamais partir dans un commit — il porte de vraies valeurs (domaine,
# e-mail).
/setup.conf

# === Tests ===
/.phpunit.cache/

# === IDE / éditeurs ===
/.idea/
/.vscode/
*.swp
*.swo